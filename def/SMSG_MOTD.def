# SMSG_MOTD - SkyFire 5.4.8 bitpacked
endian: little
header:
data:
    line_count: bits, 4BI
    loop €line_count to €lines_meta:
        line_len: bits, 7BI
    flushbit
    loop €line_count to lines:
        line: €lines_meta[i].line_len's
